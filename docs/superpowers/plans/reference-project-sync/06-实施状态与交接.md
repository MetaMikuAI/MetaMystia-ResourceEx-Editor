# 实施状态与交接

## 1. 当前代码状态

- 应用源码仍位于 `src/`，根 `app/` 目标结构尚未建立。
- 12 个编辑路由仍由 `src/app/**` 组合。
- ResourceEx 导入、归一化、资产状态、序列化、导出和 dirty 生命周期仍集中在 `DataContext.tsx`。
- 业务调用者仍依赖 Design 组件 barrel 和 `@/design/ui/utils`。
- `src/design/ui/utils/cn.ts` 只转发 `@heroui/theme` 的 `cn`；这是迁移期兼容层。
- [11-依赖Patch迁移矩阵.md](./11-依赖Patch迁移矩阵.md) 定义的目标 patch 尚未复制或登记。
- `@heroui/input`、`@heroui/select` 与 `client-only` 尚未成为直接依赖。

## 2. 正式规范入口

- 目标架构与固定设计：[02-目标架构与迁移方案.md](./02-目标架构与迁移方案.md)
- 实施 Task 与文件变更：[03-实施步骤与文件规划.md](./03-实施步骤与文件规划.md)
- 依赖和配置政策：[04-依赖与工程配置.md](./04-依赖与工程配置.md)
- 验收要求：[05-验证与回归清单.md](./05-验证与回归清单.md)
- UI/交互设计：[07-UI与交互迁移设计.md](./07-UI与交互迁移设计.md)
- ResourceEx 内核：[08-ResourceEx内核与状态设计.md](./08-ResourceEx内核与状态设计.md)
- 文件 owner：[09-文件与所有权迁移矩阵.md](./09-文件与所有权迁移矩阵.md)
- 非业务偏差：[10-非业务能力同步与偏差台账.md](./10-非业务能力同步与偏差台账.md)
- Patch 矩阵：[11-依赖Patch迁移矩阵.md](./11-依赖Patch迁移矩阵.md)

## 3. 未实施范围

- 确定性 ResourceEx 夹具与行为基线；
- 根 App Router 过渡壳；
- declarations、shared、browser infrastructure；
- Design/theme 与 `cn` 调用者迁移；
- ResourceEx domain/archive/assets/Provider；
- App Shell、公告与共享编辑器 UI；
- 12 个业务编辑器；
- 单路径 `app/` 收口、依赖清理和完整回归。

## 4. 实施入口

1. 重新记录两个仓库的 HEAD、工作区状态和相交文件 diff。
2. 执行 [03-实施步骤与文件规划.md](./03-实施步骤与文件规划.md) Task 1，建立可重复的 ResourceEx 行为夹具。
3. Task 1 验收后执行 Task 2，一次建立根 App Router 过渡壳、依赖和 Patch 矩阵登记的文件。
4. 每个 Task 完成后更新本文件的当前代码状态、已完成范围、检查结果和下一入口。

## 5. 当前验证状态

当前没有项目 TypeScript、构建、测试或浏览器行为的有效验收记录。实施阶段按 [05-验证与回归清单.md](./05-验证与回归清单.md) 执行并记录实际结果；不能依据文档检查推断运行时正确性。
