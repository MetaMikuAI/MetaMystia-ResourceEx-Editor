# 非业务能力同步与偏差台账

## 1. 总则

两个项目业务完全不同，不机械复制参考仓库，也不把参考目录当作模板整包搬入。同步单位是“本项目实际需要的同类非业务能力及其最小依赖闭包”。

选中一项能力后：

1. 固定参考提交中的实现、行为和文件边界是默认基线；
2. 先复用参考源码，再适配 import、目标业务契约和运行环境；
3. 不能仅因当前草案更简单或调用者较少而另写弱化实现；
4. 不迁入没有本项目调用者、只服务参考业务或会制造死代码的模块；
5. 每个实质偏差必须在本文件登记参考文件、目标文件、差异和约束原因。

选中前先过必要性门：

- 是否有目标调用者或已确认的用户体验/工程要求；
- 是否承担可观察行为，而不是只为参考目录或实现细节服务；
- 浏览器/框架原生能力或局部常量是否已经足够；
- 新抽象、全局声明和依赖的成本是否与收益相称。

参考一致性只约束通过必要性门的能力，不能反过来成为引入能力的理由。

分类：

| 标记 | 含义                                                                   |
| ---- | ---------------------------------------------------------------------- |
| S    | 同步：能力与依赖闭包适用，保持参考实现和行为                           |
| A    | 适配：保留非业务机制，仅替换本项目业务契约、路径、环境或明确浏览器目标 |
| E    | 排除：只服务参考业务或与目标工程约束冲突                               |
| U    | 未迁入：通用但本项目没有调用者，不为目录对称制造死代码                 |

“U” 不表示自行重写；将来若本项目需要该能力，应重新以固定参考文件为起点。

## 2. memoize 必要性

参考基线 `d3b06126676430943856dfadc0bc8170920f9f72` 中：

- `app/design/theme/colors/utils.ts` 导入 `app/shared/utilities/cache/memoize.ts`；
- `swapColorScale` 由使用 `WeakMap` key 的 `memoize` 包装；
- 本项目当前 `src/design/theme/colors/utils.ts` 也保持同一调用关系。

调用边界：

- `swapColorScale` 只在 `semantic.ts` 模块初始化时使用；
- 输入只有固定的 `colors.black`、`colors.blue`；
- 返回对象立即被展开或读取属性，没有引用身份观察者；
- 缓存不在运行时热路径上提供可验证收益。

目标不迁入 memoize。`swapColorScale` 保持普通纯函数，`semantic.ts` 维持直接调用，不增加 black/blue 反转色阶常量。这里保留参考算法和输出，只删除没有必要的缓存机制；这些小规模计算仅发生在模块初始化，不需要另做性能优化。

## 3. Design 与 theme

| 参考能力/文件组                                                     | 分类 | 目标处理                                                                   |
| ------------------------------------------------------------------- | ---- | -------------------------------------------------------------------------- |
| `design/preferences/{contracts,DesignPreferencesContext}`           | S/A  | 组件与 hook 同步；目标 Provider 按本项目固定提供高外观，不接参考偏好 store |
| `theme/colors/constants/**`、`palette.ts`、`semantic.ts`            | S    | 保持参考实现、命名和颜色计算                                               |
| `theme/colors/utils.ts`                                             | A    | 保持反转算法；去掉 memoize，`semantic.ts` 维持直接调用                     |
| `theme/colors/rating/**`                                            | A    | 实现同步；与参考相同依赖本项目 `domain/evaluation/types.ts`                |
| `theme/styles/**`、`theme/index.ts`、`theme/types.ts`               | S/A  | 实现同步；背景资源 URL 按本项目静态根路径适配                              |
| `theme/runtime/{constants,styles,themeScript,useTheme,types}`       | S/A  | 保持主题解析、storage、事件和 theme-color；媒体查询按最新浏览器目标适配    |
| `theme/runtime/accountSync.ts`                                      | E    | 只服务参考账户同步                                                         |
| Button/Card/Dropdown/Input/Modal/Popover/Tooltip 等选中 Design 叶子 | S    | 整个文件及内部依赖闭包同步，业务层通过组合接入                             |
| Heading/Placeholder/ScrollMask/ScrollState 等目标新增叶子           | S    | 从参考文件同步，不根据草案重新实现                                         |
| resourceEditor Select                                               | A    | HeroUI 交互基线来自参考；泛型值、sections 和错误语义按本项目业务适配       |
| `app/assets/fonts/**`                                               | E/A  | 不迁入；目标保留 `next/font/google`，由原样 Next Patch 改写构建请求域名    |
| FontAwesome、QR、siteInfo、Loading、TimeAgo、Rednote                | E/U  | 参考业务/依赖专用或本项目无调用者                                          |
| OL、Pagination、Snippet、Avatar、AutoHideTooltip 等通用叶子         | U    | 仅在目标调用者出现时按参考实现迁入                                         |

Design 叶子是否选中由目标业务调用者决定；一旦选中，不能只模仿 className 或外观而丢失参考的 motion、reduced-motion、blur、portal、滚动和焦点行为。

## 4. Shared

| 参考文件                                              | 分类 | 目标处理                                                         |
| ----------------------------------------------------- | ---- | ---------------------------------------------------------------- |
| `shared/react/useHydrated.ts`                         | S    | 同步参考 hook；需要 callback 的旧调用者另用 effect 组合          |
| `shared/utilities/cache/memoize.ts`                   | U    | 目标没有必要调用者，不迁入                                       |
| `shared/utilities/objects/cloneJsonObject.ts`         | S    | ResourceEx 纯 JSON 深拷贝复用该实现，序列化规则仍归 domain       |
| `shared/utilities/interaction/checkA11yConfirmKey.ts` | U    | 12 个 surface 中 4 个本来就是按钮、8 个是非 press Card；无调用者 |
| collections/cssUnits/number/string/object 其他工具    | U    | 仅发现语义等价的目标调用者时逐叶子迁入                           |
| pinyin/search/sortPinyin                              | E/U  | 本项目没有对应搜索能力，不迁入其依赖                             |
| `shared/site/**`                                      | E    | 站点元数据属于参考业务                                           |
| `useDocumentTitle`、`useThrottle`                     | U    | 当前目标没有调用者                                               |

不新增 `lodash` 或 `@types/lodash`。若未来出现真正需要 memoize 的目标调用者，再从固定参考实现重新评估；不能以主题初始化的当前调用面预先引入它。

## 5. Browser infrastructure

| 参考文件/能力                                   | 分类 | 目标处理                                                             |
| ----------------------------------------------- | ---- | -------------------------------------------------------------------- |
| `storage/contracts.ts`                          | A    | 只保留目标调用者实际需要的 get/set/remove 契约                       |
| `storage/safeStorage.ts`                        | A    | 保留 local/session/memory、无抛错和失败后防陈旧读取；按 D09 缩小协议 |
| safeStorage optional local-cache exports        | E/U  | 只服务未迁入的账户 snapshot/cache                                    |
| `compatibility/mediaQuery.ts`                   | E/A  | 明确只为 Safari < 14；本项目最新浏览器目标下直接使用标准事件 API     |
| `storage/createPersistMiddleware.ts`            | E/U  | 本项目不使用 Zustand 持久化 store                                    |
| `crossTab/**`                                   | E/U  | 本项目不迁入参考 store 同步、IndexedDB lock 和账户状态               |
| `compatibility/checkCompatibility.ts`、platform | E/U  | 旧浏览器/平台业务未使用                                              |
| `dom/setScriptUrlTag.ts`、`waitDomReady.ts`     | U    | 当前目标没有脚本注入或 DOM-ready 调用者                              |
| `browser/crypto/idRangeSignature.ts`            | A    | 本项目专属能力；沿用现有签名协议，不冒充参考实现                     |
| `browser/images/readImageDimensions.ts`         | A    | 本项目四个图片校验调用者共用；统一 transient object URL 生命周期     |

本项目当前三个调用面只读写主题、公告已读版本和资产视图偏好，不使用 `length`、`key`、`clear`、mode、managed-key 枚举或 optional local cache。目标不复制参考账户/跨标签 store 驱动的完整恢复协议，但必须修复当前实现在持久层写入失败后可能重新读回陈旧值的问题。

D09 的目标行为固定为：

- 公开面只有 `getItem`、`setItem`、`removeItem`；
- 初始化按 localStorage → sessionStorage → memory 选择；内部 shadow Map 与 tombstone Set 只跟踪本实例实际访问过的 key，不枚举全局 storage；
- `getItem` 成功得到字符串时写入 shadow 并清除 tombstone；成功得到 `null` 时删除 shadow 并写入 tombstone；
- 首次 `getItem` 抛错且该 key 没有 shadow 时，立即写入 tombstone 并返回 `null`；
- `setItem` 先写 shadow 并清除 tombstone；`removeItem` 先写 tombstone 并删除 shadow，再访问持久层；
- 当前持久层任一 read/write/remove 抛错后，本实例永久降级到下一层；local 降级时用 shadow/tombstone 初始化 session，session 再失败则只用 memory；
- 降级后下层 storage 只作为 shadow 与未来写入的持久化 sink，不作为旧值 source；未访问过且 shadow 未知的 key 返回 `null`，tombstone 优先于任何下层旧值；
- 不实现参考账户场景的恢复重试、managed-key 持久化、fallback mode 持久化或 optional cache；
- hydration 后的主题、公告版本和资产视图偏好使用该 adapter；首屏 theme script 仍以 try/catch 直接读取 localStorage；
- 跨标签主题同步只在 localStorage 可用时成立，运行中降级后不伪造 cross-tab 一致性。

## 6. Declarations

| 参考声明                          | 分类 | 目标处理                                                                   |
| --------------------------------- | ---- | -------------------------------------------------------------------------- |
| `styles.d.ts`                     | S    | 原语义同步                                                                 |
| `collections.d.ts`                | U/A  | 不复制整份全局增强；theme 需要的 `Prettify` 改为局部类型                   |
| `stdlibOverrides.d.ts`            | S    | 保留 `JSON.parse(): unknown` 与 `Array.isArray(): unknown[]` 边界          |
| `dom.d.ts`                        | A    | 保留 Modal portal 和选中 Design 叶子所需类型；移除未安装 HeroUI Table 类型 |
| `tooling.d.ts`                    | E    | 只声明 ESLint 插件，本项目不使用 ESLint                                    |
| `environment.d.ts`                | E    | 参考服务端、部署和账户环境变量                                             |
| documentPictureInPicture/WebAuthn | E/U  | 本项目无对应能力                                                           |
| 本项目 `*.pem` 声明               | A    | 放入 `assets.d.ts`，属于本项目 ID 签名资产边界                             |

声明不能为目录对称整包复制；它们会改变整个 TypeScript 程序的类型系统，必须按实际引用和依赖审查。

## 7. Globals、布局与工程配置

| 区域                 | 分类 | 目标处理                                                                        |
| -------------------- | ---- | ------------------------------------------------------------------------------- |
| `tsconfig.json`      | S/A  | 严格选项同步；alias/include 按根 `app/` 和实际文件适配                          |
| `tailwind.config.ts` | S/A  | 同步 HeroUI 自动发现和主题组合；路径、静态资源 URL 按本项目适配                 |
| `next.config.ts`     | A    | 固定静态 export；同步 strict/typed routes；保留 PEM rule，排除全栈运行分支      |
| `app/globals.scss`   | S/A  | 相同通用规则按参考块同步；本项目背景/像素图规则归目标 owner                     |
| `app/layout.tsx`     | A    | 复用主题脚本、provider 和 modal portal；保留目标 `next/font/google` 与 AppShell |
| `app/providers.tsx`  | A    | HeroUI/Design provider 机制同源；ResourceEditor/announcement 按本项目组合       |
| `app/(pages)/**`     | S/A  | 采用参考 Route Group 组织业务 shell；不复制参考业务 layout，目标 URL 保持不变   |

`next.config.ts` 不同步参考账户、服务端、CDN、analytics、self-hosted 或构建身份逻辑。用户明确确认 `SKIP_LINT` 是其需要的环境变量，因此目标按 D14 保留迁移前已有的 ESLint、TypeScript 与 webpack memory 分支；这不是从参考项目复制的全栈运行模式。

Task 4 将 12 个既有业务 shell 机械移入 `app/(pages)/`，但保留根 `app/page.tsx` 的客户端替换、根 layout/providers 和每个 shell 的 export 内容。Route Group 本身不产生 URL 段；目标只采用该组织边界，不声称同步了参考项目的业务路由或逐页 layout。

## 8. 依赖 patch 边界

选中包、版本、Patch 文件、复制规则与排除证据只在 [11-依赖 Patch 迁移矩阵](./11-依赖Patch迁移矩阵.md) 维护。选中 Patch 从固定参考提交原样复制并按包名登记，不允许在目标仓手改；本文件不重复数量或包清单。

Select 接受参考 patch 移除自动滚动后的行为，不在业务适配器中恢复该行为。

## 9. 当前偏差台账

| 编号 | 参考基线                                                        | 目标偏差                                                                 | 原因                                                                  |
| ---- | --------------------------------------------------------------- | ------------------------------------------------------------------------ | --------------------------------------------------------------------- |
| D01  | `theme/runtime/accountSync.ts`                                  | 不迁入                                                                   | 参考账户业务不存在                                                    |
| D02  | `compatibility/mediaQuery.ts`                                   | 直接使用标准 add/removeEventListener                                     | 明确只支持最新稳定浏览器                                              |
| D03  | DesignPreferences 接参考 preferences store                      | Provider 固定 `isHighAppearance: true`                                   | 本项目不提供设置/账户持久化                                           |
| D04  | `dom.d.ts` 全量全局类型                                         | 只保留选中 Design 闭包所需类型                                           | 不引入 HeroUI Table 和未使用组件依赖                                  |
| D05  | 参考 Next export/standalone/Node 多运行模式                     | 固定 `output: 'export'` + PEM loader                                     | 产物由 Nginx 等静态服务器直接托管                                     |
| D06  | 参考 PostCSS 生产兼容链                                         | 保留现有 preset-env，不新增 flexbugs                                     | 目标工程保留既有链，排除额外兼容层                                    |
| D07  | 参考整个 shared/browser/design 目录                             | 只迁入目标能力及最小依赖闭包                                             | 业务不同，避免死代码和参考业务渗入                                    |
| D08  | `swapColorScale` 使用 memoize                                   | 普通纯函数，`semantic.ts` 直接调用                                       | 仅初始化时小规模计算，无缓存或预计算必要                              |
| D09  | 完整 safeStorage 恢复/optional cache                            | 保留三级退化与一致性核心，缩小公开接口                                   | 目标只存三个标量偏好，无账户/store 恢复                               |
| D10  | 全量 `collections.d.ts` 全局增强                                | 仅在 theme 内定义所需 `Prettify`                                         | 其余 Map/WeakMap/ValueCollection 无调用者                             |
| D11  | 参考 Design 只有 Input wrapper                                  | 新增同 owner 的 Textarea wrapper                                         | 目标存在 textarea 字段，需保持同一 UI 策略                            |
| D12  | 参考通过 hoisted linker 解析 `color2k`                          | 目标使用 isolated linker + public hoist                                  | 保持目标 linker，同时满足 theme 导入解析                              |
| D13  | 参考同步 `app/assets/fonts/**` 与字体 CSS                       | 保留目标 `next/font/google` + Next Patch                                 | 沿用目标字体加载方式，不复制字体资源目录                              |
| D14  | 参考/原定目标不保留 `SKIP_LINT` 忽略分支                        | 保留目标迁移前 `SKIP_LINT` 环境变量逻辑                                  | 用户明确要求保留其需要的环境变量与构建开关                            |
| D15  | 参考/原定目标直接声明 `client-only@0.0.1`                       | 不声明为目标直接依赖                                                     | 用户明确确认正常 import 不需要重复安装                                |
| D16  | Task 1 旧 Context 把 `LICENSE.md` 当普通文件建立 persistent URL | archive reader 只把 LICENSE 读入 license state，不进入 file/URL snapshot | 落实 08 的 entry 唯一 owner；避免无预览用途的 URL 与重复归属          |
| D17  | 原定最终将 TypeScript include 收窄为显式 `app/**`               | 保留五项宽范围 `**/*.mjs`、`**/*.ts`、`**/*.tsx`、Next types/include     | 用户明确要求保留其按参考项目调整的版本；`src/` 已删除                 |
| D18  | 参考 Select 的受控 key 由当前 collection item 提供              | 目标 adapter 在业务值暂时不属于 items 时提交空 selected-key set          | 避免 HeroUI invalid-key warning，不改写保存的业务值                   |
| D19  | 参考 Modal 在默认 portal effect 执行前直接按 `isOpen` 打开      | 未显式传 portal 时，等待默认 portal 查询完成后再向 HeroUI 打开           | 防止条件挂载即打开时先落到 body、迁移 portal 后祖先残留 `aria-hidden` |
| D20  | 目标根 metadata 未声明图标，浏览器隐式请求 `/favicon.ico`       | metadata 指向既有 `public/assets/icon.png`                               | 关闭纯静态站点唯一的成功路径 404，不新增资产或运行时能力              |

选中的 Design 叶子应保持参考实现，仅允许路径和已登记边界变化。主题色阶 D08 与 storage D09 的调用边界均已明确，目标适配不保留参考业务不需要的复杂度。

D16 的可观察兼容影响已经固定：`archive-license-empty.zip` 旧基线为 7 个 persistent URL（6 个普通文件加空 LICENSE），Task 5 后为 6 个；missing/empty 连续导入累计从 `13/6` 变为 `12/6`，创建空白后为 `12/12`。LICENSE 的状态与导出语义不变：缺失和空内容都归一化为空字符串并不生成导出 entry，非空内容按原字节写回。

D19 只改变默认 portal 尚未解析完成的首次提交时序。显式传入 `portalContainer` 的调用者仍可立即打开；默认调用者延后一轮 effect 后直接在 layout 已提供的 portal 内打开。Task 9 的真实导出校验 Modal 在修复前可见但其祖先为 `aria-hidden="true"`，修复后 `role=dialog`、`aria-modal=true`，最近 portal 为 `#modal-portal-container` 且无 `aria-hidden` 祖先；取消、Escape、返回修改、忽略后导出和焦点回收路径保持。

D19 的默认 portal 状态使用 `undefined`（尚未完成查询）、`HTMLElement`（找到目标）和 `null`（查询完成但目标不存在）三态；缺失 portal 时仍使用 HeroUI body fallback，不会永久抑制打开。D20 只复用已经由 Navbar 和静态资产矩阵使用的 184×184 PNG；最终 clean-copy Chrome 对图标请求返回 200，console warning/error 为 0。

Task 9 同时把 AppNavbar dropdown 从 `DropdownItem as={NextLink}` 改为参考 App Shell 使用的 collection `onAction` 导航模式：`textValue` 继续属于 collection metadata，不再透传到 DOM；12 个 URL 和客户端不丢状态导航不变。该项回归参考交互基线，不另记偏差。D18 只处理目标业务可能短暂保存旧 Select 值、而当前选项已经为空或改变的边界；adapter 仍保留原值契约，选项恢复时会重新显示对应值。

## 10. 实施验证

每个非业务切片：

1. 记录固定参考文件和目标文件；
2. 先同步参考文件及其最小依赖闭包；
3. 将业务接入适配作为独立工作区 diff 呈现，不混写不可解释的重构；
4. 用 no-index/内容 diff 审查所有非 import、类型 owner 和已登记偏差的变化；
5. 新偏差先写入本台账，再实施；
6. 运行目标调用者的 TypeScript、格式、构建和浏览器行为检查。

验收不是“目录看起来相似”，而是：

- 已选能力的核心算法和交互行为可追溯到固定参考文件；
- 所有差异都能归因于本项目业务、明确工程约束或目标调用面；
- 没有从当前草案继续保留一套语义重复的非业务实现；
- 没有为参考目录对称引入无调用者代码。
