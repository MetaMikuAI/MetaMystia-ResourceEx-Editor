# 实施步骤与文件规划

每个 Task 是独立审查门。文档定稿不改变清单状态；实施必须获得独立授权。

## Task 1：冻结行为基线与夹具

**Files:**

- Update: `docs/superpowers/plans/reference-project-sync/*.md`
- Create conditionally: `fixtures/resource-pack/**`

**Produces:** 可重复比较导入状态、导出 JSON、ZIP 文件列表和资产字节的基线。

- [x] 记录执行开始时两个仓库的精确 HEAD 和工作区状态。
- [x] 若参考仓库已前进，仍读取固定提交 `d3b06126`，除非任务基准明确升级。
- [x] 为 12 个路由列出至少一个核心增删改路径和数据字段。
- [x] 准备最小包、旧字段包，以及分别不带 LICENSE、带空 LICENSE、带非空 LICENSE 的资产目录包。
- [x] 记录当前实现归一化 JSON、导出 JSON、ZIP entry 和资产 SHA-256。
- [x] 在真实浏览器记录导入、编辑、导出、未保存拦截和对象 URL 清理。
- [x] 将夹具位置、哈希、浏览器版本和结果写入验证文档。

## Task 2：建立可运行的根 App Router 过渡壳、依赖与工程配置

**Create/sync:**

- `app/{layout,providers,page}.tsx`
- `app/{asset,beverage,character,clothes,dialogue,event,food,info,ingredient,merchant,mission,recipe}/page.tsx`
- `app/globals.scss`
- [11-依赖 Patch 迁移矩阵](./11-依赖Patch迁移矩阵.md)登记的 patch 文件

**Modify:**

- `next.config.ts`
- `package.json`
- `tsconfig.json`
- `tailwind.config.ts`
- `pnpm-lock.yaml`

- [x] 根 `app/` 接管路由时一次性建立 layout、providers、根重定向和全部 12 个 route shell；shell 暂时组合尚在 `src` 的现有 owner。
- [x] 以逐文件审查和导入扫描验证 app/design/domain/features/infrastructure/shared 单向依赖。
- [x] 迁移期路径映射使用 `@/* -> ["./app/*", "./src/*"]`，TypeScript include 与 Tailwind content 同时覆盖 `app`/`src`；最终删除 `src` 后才收紧为单一 `./app/*`。
- [x] 为源码直接调用提前增加 `@heroui/input`、`@heroui/select`；按用户要求不把 `client-only` 声明为目标直接依赖（D15）。
- [x] 从固定参考提交原样复制 Patch 矩阵登记的文件，并按包名写入 `pnpm.patchedDependencies`；不手改 patch 内容。
- [x] 运行 `pnpm install` 更新 lockfile，安装必须无 patch 失败；具体清单与排除证据见 [11-依赖Patch迁移矩阵.md](./11-依赖Patch迁移矩阵.md)。
- [x] Next 配置同步适用的 `reactStrictMode`/typed routes，保留静态 export、PEM loader，以及用户明确要求的现有 ESLint/TypeScript/webpack `SKIP_LINT` 分支（D14）。
- [x] 不复制参考的 export/standalone 条件分支、Server Action、headers、CDN/assetPrefix、请求时环境或其他 Node runtime 配置；`output: 'export'` 始终生效。
- [x] `app/layout.tsx` 保留现有 `next/font/google` 的 Noto Sans、Noto Sans Mono、Noto Sans SC 配置、CSS 变量与 `<html>` className 接入；不复制参考字体目录。
- [x] 验证根 `app` 接管后 `/` 与全部 12 个编辑 URL 仍可访问，再执行 TypeScript、focused Prettier 和 diff 检查。

## Task 3：迁移 declarations、shared、browser infrastructure

**Create:**

- `app/declarations/styles.d.ts`
- `app/declarations/assets.d.ts`
- `app/declarations/dom.d.ts`
- `app/declarations/stdlibOverrides.d.ts`
- `app/shared/react/useHydrated.ts`
- `app/infrastructure/browser/storage/contracts.ts`
- `app/infrastructure/browser/storage/safeStorage.ts`
- `app/infrastructure/browser/crypto/idRangeSignature.ts`
- `app/infrastructure/browser/crypto/public.pem`
- `app/infrastructure/browser/images/readImageDimensions.ts`

**Remove after caller migration:**

- `src/types/globals.d.ts`
- `src/hooks/**`
- `src/utilities/safeStorage.ts`
- `src/lib/crypto.ts`
- `src/lib/keys/public.pem`

**Transitional modify:** `src/utilities/index.ts` 删除 safeStorage 导出，仅暂时保留仍被旧 theme 使用的 memoize；Task 4 与 memoize 一起删除。

- [x] 保留 PEM module 声明。
- [x] `safeStorage` 保留参考三级 fallback 和失败一致性问题的处理；按 D09 删除账户恢复、managed-key 持久化和 optional cache。
- [x] `useHydrated` 同步参考实现；旧 callback 调用者另用 effect 组合。
- [x] `dom.d.ts` 只适配选中 Design 闭包，不能引入 HeroUI Table 等未使用依赖。
- [x] 不复制全量 `collections.d.ts`；theme 所需 `Prettify` 按 D10 局部定义。
- [x] 保持 ID 签名消息、范围、PKCS#8 限制和返回值。
- [x] 更新所有调用者，不保留旧 barrel。
- [x] 验证 local/session/memory、健康层外部更新可见、运行中读写删除失败降级和不会读回陈旧持久值；覆盖首次 `get` 失败、成功读取 `null` 后降级，以及降级后读取未知 key。

## Task 4：同步 design

**Sync/adapt selected capability closure:**

- `app/design/preferences/**`
- `app/design/theme/**`（排除 `runtime/accountSync.ts`）
- `app/design/ui/components/{constant,scrollState}.ts`
- `app/design/ui/components/{button,card,dropdown,heading,input,modal,placeholder,popover,scrollMask,scrollShadow,switch,tooltip}.tsx`
- `app/design/ui/hooks/{useMotionProps,useReducedMotion}.ts`

**Create as an explicit target adaptation:**

- `app/domain/evaluation/types.ts`
- `app/design/ui/components/textarea.tsx`

**Route organization added during implementation:**

- `app/{asset,beverage,character,clothes,dialogue,event,food,info,ingredient,merchant,mission,recipe}/page.tsx` → `app/(pages)/<route>/page.tsx`；URL 与 shell export 内容不变，不新增 group layout。

Avatar/Badge/FadeMotionDiv/Link/Pagination/PressElement/Snippet 等没有最终目标调用者，不迁入。

**Remove after all callers/config migrate:** `src/design/**`（Task 6 仍有真实调用者的 `ui/components/{select,navbar}.tsx` 暂留）、`src/types/evaluation.d.ts`

**Remove after color caller migration:** `src/utilities/{memoize,index}.ts`

**Transitional modify:** `src/types/index.ts` 删除 evaluation 导出，ResourceEx 导出保留到 Task 5。

- [x] 建立当前 props → 参考 props → 调用者改法矩阵。
- [x] 迁移前完整读取 `src/design/theme/styles/index.ts` 与 `tailwind.config.ts` 及其工作区 diff，把当前主题导出、资源路径和 HeroUI 自动发现逻辑合入新 owner 后才能删除旧文件。
- [x] 每个选中叶子先同步固定参考文件，再单独应用已登记的业务/环境适配。
- [x] theme runtime 移到 `design/theme/runtime`。
- [x] `swapColorScale` 改为普通纯函数，`semantic.ts` 保持直接调用；删除无调用者 memoize，不增加预计算常量。
- [x] 先迁入本项目 `domain/evaluation/types.ts`，再让参考 rating theme 依赖该叶子；领域键不归 Design 声明。
- [x] 保留 system/light/dark、跨标签 storage 和 theme-color。
- [x] 使用标准 `MediaQueryList.addEventListener`。
- [x] Providers 固定提供 `isHighAppearance: true`，不引入偏好 store。
- [x] `cn` 全部改从 `@heroui/theme` 导入。
- [x] Input 同步参考文件；Textarea 以同一外观、reduced-motion 和 `inputWrapper` 策略包装 HeroUI `Textarea`，并登记为 D11。
- [x] 不迁入无目标调用者的通用叶子；不以调用者少为理由简化已选中的参考实现。
- [x] 4 个原生 `<button>.surface-pressable` 迁到 Design Button；8 个包裹独立子按钮的 `<div>` 迁到非 pressable Card，不能把整个容器伪装成按钮。
- [x] 隔离页面逐项验证 4 个 Button 与 8 个 Card 的运行时 DOM 语义；6 个悬浮删除按钮同时验证键盘焦点显隐和 390×844 touch 可达性。
- [x] `info/IdRangeEditor.tsx` 的原生 `<dialog>` 迁到 Design Modal。
- [x] 12 个业务 route shell 移入 `app/(pages)/`，不改变 URL、不新增 group layout。
- [x] 验证所有 Modal 调用者、portal、滚动 mask 及 light/dark/system/reduced-motion。

## Task 5：拆分 ResourceEx domain 与 archive/asset state

**Create:**

- `app/domain/resourcePack/contracts/{character,dialogue,event,items,merchant,mission,resourceEx}.ts`
- `app/domain/resourcePack/contracts/resourcePackWire.ts`
- `app/domain/resourcePack/constants.ts`
- `app/domain/resourcePack/createBlankResourcePack.ts`
- `app/domain/resourcePack/normalization.ts`
- `app/domain/resourcePack/serialization.ts`
- `app/domain/resourcePack/validation.ts`
- `app/domain/resourcePack/assetReferences.ts`
- `app/shared/utilities/objects/cloneJsonObject.ts`
- `app/features/resourceEditor/client/archive/readResourcePackArchive.ts`
- `app/features/resourceEditor/client/archive/writeResourcePackArchive.ts`
- `app/features/resourceEditor/client/archive/downloadResourcePack.ts`
- `app/features/resourceEditor/client/archive/contracts.ts`
- `app/features/resourceEditor/client/assets/useAssetStore.ts`
- `app/features/resourceEditor/client/assets/contracts.ts`
- `app/features/resourceEditor/client/validation/validateResourcePackForExport.ts`
- `app/features/resourceEditor/client/state/ResourceEditorProvider.tsx`
- `app/features/resourceEditor/client/state/useResourceEditor.ts`
- `app/features/resourceEditor/client/state/contracts.ts`

**Remove after caller migration:**

- `src/components/context/DataContext.tsx`
- `src/components/context/utils.ts`
- `src/types/{resource.d.ts,index.ts}`
- `src/lib/constants.ts`

- [x] 将 467 行 `resource.d.ts` 按实体边界拆为叶子 `.ts` 契约，不建聚合 barrel；保持外部 JSON 拼写和既有 public-boundary 类型名。
- [x] 解析边界使用只属于 reader 的 `IResourcePackWire`：允许 `packInfo`、十个集合与遗留顶层包信息缺失；编辑器状态 `ResourceEx` 则始终具有 `packInfo` 和十个必需数组。
- [x] ResourceEx 纯 JSON 深拷贝复用参考 `cloneJsonObject`，业务裁剪仍归 domain。
- [x] 固定解析链为 `unknown → 校验 wire → IResourcePackWire → 归一化 → ResourceEx`；集合缺失或为 `null` 时归一化为 `[]`，其他非数组值按路径报错。
- [x] `packInfo` 存在时忽略遗留顶层包信息；仅在 `packInfo` 整体缺失时由遗留字段构造。LICENSE 只从 `LICENSE.md` 读取，不读取遗留 `packInfo.license`。
- [x] 导出清理/排序/CRLF 成为不修改编辑状态的纯函数。
- [x] 序列化始终输出十个集合；缺失或 `undefined` 的可选实体字段省略，契约允许的 `null` 保留，空集合输出 `[]`。
- [x] JSZip/FileSaver 限制在 archive adapter。
- [x] 领域校验只包含 ID、Label、结构与引用规则；Web Crypto 仅在 `infrastructure/browser/crypto`，导出校验由 feature orchestration 组合领域问题、资产路径与签名验证。
- [x] 持久资产预览 URL 仅由 `useAssetStore.ts` 创建和 revoke；Provider 只组合该 hook。瞬时图片尺寸读取统一通过 `readImageDimensions`，并在成功、失败或取消路径 revoke。
- [x] Provider 只组合纯函数与 adapter，并保留 `beforeunload`；archive reader 不创建对象 URL。
- [x] 归档 entry 遵循固定分类：`ResourceEx.json` 只归 JSON reader；`LICENSE.md` 只归 license；`__MACOSX/**` 忽略；`assets/` 根目录始终存在，`assets/**` 文件始终回写且目录进入 folder snapshot；非 `assets/` 普通文件保留 snapshot、仅在 JSON 引用时回写；非 `assets/` 目录忽略。
- [x] `license` 为空字符串时不生成 `LICENSE.md`；非空时按原文本写回。
- [x] 删除 `setData` + `setHasUnsavedChanges` 双调用，所有数据/LICENSE/资产 mutation 原子标记 dirty。
- [x] 导入/创建成功置 clean，取消/失败不替换 state；成功导出下载后置 clean。
- [x] 固定 Branch `price` 当前会被导出裁剪的结果，不在结构迁移中顺带修复。
- [x] 对固定夹具比较结构、ZIP entry、资产哈希。

首轮独立审查未通过：初版 wire reader 只校验集合外壳并依赖宽断言，导出没有绑定 validation revision，asset folder 更新可能被旧闭包覆盖，Label 保留字缺少领域/导出双重硬门，引用集也不是从最终裁剪后的 JSON 视图生成。首轮修复改为 path-aware 深层 reader、revision-bound snapshot/export、transactional asset maps 与 ref-backed folder owner，并由扩展后的 domain/archive harness、state/assets harness 和 focused browser lifecycle probe 覆盖。

第二轮复审仍未通过：深层 reader 误把 legacy guest collections、Food `tags`/`banTags` 和 Beverage `tags` 当成 wire required，`packInfo.license` 仍参与类型校验，且卸载后的旧 asset/import callback 仍会进入 disposed registry 或提交路径。第二轮修复恢复缺失字段默认值而继续拒绝 present wrong-type；`packInfo.license` 任意值均完全忽略；Object URL registry、hook mutation 与 Provider async import 均增加 StrictMode-compatible lifetime guard。第二轮修复已由原独立审查者复核批准，Task 5 验收完成。

## Task 6：迁移共享编辑器 UI、App Shell、公告

**Move/adapt:**

- `src/components/common/**` → resourceEditor components、appShell 或 announcements
- `src/components/icons/**` → 实际 feature owner

**Create/adapt:**

- `app/features/resourceEditor/client/components/select/Select.tsx`
- `app/features/resourceEditor/client/components/confirm/{ConfirmDialog,ConfirmPopover}.tsx`
- `app/features/resourceEditor/client/components/layout/{EditorHeader,EditorPanel,EditorWorkspace}.tsx`
- `app/features/appShell/client/AppNavbar.tsx`
- `app/features/appShell/client/BackToTop.tsx`

- [x] 外观组件与 ResourceEx 语义组件分开。
- [x] Select 底层使用 `@heroui/select`，保留受控值、分组、禁用项、textValue、宽度、键盘和主题行为。
- [x] TextInput/TextArea 调用者迁到基于 `@heroui/input` 的 Design Input/Textarea；Textarea 是 D11 目标适配。
- [x] Select 使用已原样迁入的参考 patch；不额外恢复 patch 移除的打开时自动滚动。
- [x] Design Button/可按压 HeroUI 调用统一使用 `onPress`；原生 HTML `<button>` 才保留 `onClick`。
- [x] 将 35 个原生 `alert`/`confirm` 按 UI 专题矩阵迁到 Popover、Modal 或内联反馈。
- [x] Navbar 直接组合 HeroUI/design 叶子模块，导出动作只调用 feature 的 `validateResourcePackForExport` orchestration，不直接访问领域校验或 crypto。
- [x] `info/IdRangeEditor.tsx` 使用 Design Modal，并由 info feature 组合领域范围规则与签名 adapter。
- [x] `PortraitUploader.tsx`、`SpriteUploader.tsx` 改用 `readImageDimensions`，不直接创建瞬时对象 URL。
- [x] 保留桌面/移动导航、上传、创建、导出、公告、GitHub。
- [x] 验证键盘、焦点、移动菜单和导出校验 Modal。

## Task 7：迁移数据与 12 个编辑器

**Move/adapt:**

- `src/data/**` → `app/domain/data/**`
- `src/components/{asset,beverage,character,clothes,dialog,event,food,info,ingredient,merchant,mission,recipe}/**` → `app/features/resourceEditor/client/editors/**`

- [x] 先迁移无跨编辑器写依赖的静态数据。
- [x] 顺序：asset → info → ingredient/food/beverage/recipe/clothes → character/dialogue/merchant → mission/event。
- [x] 每个切片更新全部调用者并删除原文件。
- [x] 为每个路由创建 feature Screen，把 selectedIndex/preview/collapse 与 CRUD 从 route 下沉。
- [x] 保留索引选择与删除后调整语义；不因迁移强制改成可能重复的 ID/Label。
- [x] 每个切片执行静态检查和对应浏览器增删改流程。
- [x] 角色/对话/任务/事件额外验证引用、分支、奖励和后置节点。
- [x] `clothes/SpriteGrid.tsx` 与 `character/editor/SpriteSet.tsx` 改用 `readImageDimensions`，不直接创建瞬时对象 URL。

## Task 8：完成 App Router、样式与单路径收口

**Finish/adapt:** Task 2 已建立的 `app/**` 过渡壳

**Modify:**

- `app/globals.scss`
- `app/layout.tsx`
- `app/providers.tsx`
- `app/page.tsx`

**Remove:** `src/`

- [ ] route 只保留组合职责。
- [ ] 所有 route 都能在构建时静态生成；交互、ZIP、storage、Web Crypto、对象 URL 和下载只在 client boundary。
- [ ] globals 只同步参考通用样式；编辑器专属样式归 feature。
- [x] layout 增加 `#modal-portal-container`；Task 4 已接入，overlay 层级和焦点回收仍按 Task 4/8 浏览器门验证。
- [ ] 删除 `.surface-pressable`；Task 4 已删除该规则与全部调用点，`.bg-checkerboard` 仍待下沉 asset feature。
- [ ] 保持 `next/font/google` 生成的 CSS 变量与 Design `fontFamily.ts` 一致。
- [ ] 保持 `/` → `/info` 和全部现有路径。
- [ ] 根页面使用客户端 replace 保持静态导出兼容，不改用需要 Next runtime 的 redirect；保留可用的无脚本 fallback 链接。
- [ ] 全部 owner 迁完后删除 `src`，将 alias/include/Tailwind content 从双路径收紧到单一 `app`，再扫描旧 alias、barrel、父级相对 import。

## Task 9：依赖清理与完整回归

**Modify:**

- `package.json`
- `pnpm-lock.yaml`
- 本文档集

- [ ] 确认全部 `cn` 调用者已直接从 `@heroui/theme` 导入，迁移期转发层不存在。
- [ ] 删除无目标调用者的 `@heroui/avatar`、badge、link、pagination、snippet。
- [ ] 确认 Task 2 新增的 `@heroui/input`、`@heroui/select` 仍有直接调用者；`client-only` 不作为目标直接依赖（D15）。
- [ ] 删除迁移后无调用者的依赖，并确认没有为单个能力引入 Lodash 或重新建立 memoize。
- [ ] 再次执行 `pnpm install`，确认 Patch 矩阵登记的条目均被 lockfile 识别且安装无失败。
- [ ] 记录新增直接依赖与 Patch 条目的解析证据。
- [ ] 执行验证文档全部静态、构建和浏览器矩阵。
- [ ] 扫描并排除 Server Action、请求时 Route Handler、cookies/headers、rewrites/proxy、默认图片优化和运行时 Node 依赖；核对 `out/` 可由静态服务器直接提供。
- [ ] agent 临时脚本、夹具副本、截图、trace 和其他输出只写入根 `.tmp/`；任务结束时关闭 Playwright/浏览器会话、开发/静态服务器和后台进程，清理本任务专用的浏览器、npm/pnpm 等缓存；`.tmp/` 普通临时文件保留并列入交接，不阻塞任务完成，用户后续另行要求时再清理。
- [ ] 写回最终文件清单、检查结果、兼容影响和未验证项。
- [ ] 对每个选中非业务文件做参考/目标内容 diff，所有实质差异均能对应偏差台账。
