# 验证与回归清单

## 1. 验证策略

项目没有 test script 或自动化测试套件。每个实施 Task 使用 TypeScript、focused Prettier、diff check、确定性夹具和对应运行时流程共同验收；静态检查不能替代运行时功能验证。

迁移设计阶段只执行文档格式、文档链接、占位标记和 diff 一致性检查，不建立项目代码或运行时通过结论。

## 2. 确定性夹具

实施前建立四类输入：

1. 最小有效 ResourceEx 包；
2. 包含旧字段/缺省字段的兼容迁移包；
3. 分别不带 `LICENSE.md`、带空 `LICENSE.md`、带非空 `LICENSE.md`，并包含 `assets/**`、被 JSON 引用与未引用的非 `assets/` 文件、嵌套空目录和 `__MACOSX/**` 的完整包。
4. 包含所有 DialogAction，且 Branch 同时具有 `price`/`text`/`jump` 的动作包。

每类记录：

- 输入文件 SHA-256；
- 归一化后 JSON；
- 导出 `ResourceEx.json`；
- ZIP entry 名称、目录标志和排序；
- 每个资产 SHA-256；
- 导入后与导出后的 LICENSE；
- 预期错误/警告。

兼容迁移包必须分别覆盖：

- `packInfo` 存在且遗留顶层包信息也存在：只采用 `packInfo`；
- `packInfo` 整体缺失：由遗留顶层字段构造；
- 集合缺失或为 `null`：归一化为 `[]`；
- 集合为其他非数组值：返回带路径的错误；
- `packInfo.license` 存在但无 `LICENSE.md`：不作为 license fallback；
- 不存在或为空的 `LICENSE.md`：归一化为 `license: ''`，再次导出时不生成该条目；
- 非空 `LICENSE.md`：文本内容原样写回；
- 规范化状态始终包含 `packInfo` 和十个必需数组，序列化也始终输出十个集合。

动作包必须把“当前导出会裁剪 Branch `price`”记录为迁移基线；本次结构迁移不能把它误写成已修复行为。

若现有样例含真实数据，只在项目根 `.tmp/` 中使用且不得提交；任务结束时保留并列入交接，不阻塞任务完成，用户后续另行要求时再清理。

## 3. 静态检查

每个实施切片：

```bash
pnpm exec tsc --noEmit
pnpm exec prettier --check <changed-files>
git diff --check
git diff --cached --check
```

最终额外运行：

```bash
pnpm build
```

记录 baseline warning 与新增 warning，不能混写。

### 非业务参考一致性

每个选中的 Design/theme/shared/browser 文件额外记录：

- 固定参考路径与目标路径；
- 参考实现的最小依赖闭包；
- 除 import、类型 owner 和偏差台账外的内容 diff；
- 每个实质差异对应的 D 编号；
- 未迁入模块没有目标调用者的扫描证据。

不能用“来自参考项目”替代必要性证明，也不能用“业务不同”解释未登记的基础行为漂移。

依赖 Patch 另按 [11-依赖 Patch 迁移矩阵](./11-依赖Patch迁移矩阵.md) 验证：矩阵选中的文件与固定提交内容完全一致，`package.json` 按包名逐项登记，`pnpm install` 无 patch 失败，lockfile 含对应 hash；不得把手改 patch 当作“适配”。

## 4. 运行时矩阵

| 区域       | 必须验证                                                                  |
| ---------- | ------------------------------------------------------------------------- |
| Archive    | 空白创建、有效 ZIP、缺 ResourceEx、非法 JSON、LICENSE、旧字段、entry 分类 |
| Asset      | 上传、覆盖、预览、复制、移动、删除、空目录、引用回写、URL revoke          |
| Unsaved    | 数据/LICENSE/资产原子触发，取消/失败不复位，成功导出复位，离页提醒        |
| Domain     | wire 归一化、Label 保留词/非法字符、ID 范围/重复、签名、引用缺失          |
| Editors    | 12 路由选择、新增、编辑、删除、排序、跨实体引用                           |
| Dialogue   | CG/BG 清空、Sound、Branch 当前裁剪结果、Goto、End、空 actions 省略        |
| Theme/UI   | system/light/dark、高外观、跨标签 storage、reduced motion、键盘与 overlay |
| Responsive | 移动导航、窄屏编辑器、桌面双栏、1920/2560 断点                            |
| Build      | 最新浏览器目标、无兼容 polyfill、静态 export、直接刷新路由                |

### 静态导出与托管

- `next build` 必须生成 `out/index.html`、`out/404.html`、12 个 route HTML、对应 RSC payload、`out/_next/**`、public assets 与 `_next/static/media` 字体。
- 用与生产一致的静态服务器规则启动 `out/`，至少直接请求 `/`、`/info`、其余 11 个编辑 URL、404、一个 `_next` chunk、一个 public asset 和一个字体文件。
- extensionless route 使用 `$uri` → `$uri.html` → `$uri/` fallback；根页客户端 replace 到 `/info`，不能依赖 Next redirect/rewrites。
- 刷新每个 route 后功能可用，客户端跨 route 导航不请求不存在的 Node/API endpoint。
- 扫描 `app/**` 与 `next.config.ts`，不得出现 Server Action、请求态 cookies/headers、动态 Route Handler、rewrites/proxy、默认图片优化或运行时 Node-only 依赖。
- 构建期 `next/font/google` 请求必须按原样 Next Patch 使用 `fonts.loli.net`/`gstatic.loli.net`，不访问原 Google Fonts 域名；浏览器运行时只加载导出产物中的自托管字体。
- 运行时不读取只能由 Node server 提供的环境变量。
- Nginx/静态服务器的缓存、压缩与安全响应头属于部署配置，不通过 Next runtime 实现。

## 5. 浏览器检查细则

- 使用真实浏览器；无可复用会话时使用 Playwright。
- 检查 console 和 network。
- 覆盖键盘打开、选择、关闭和焦点回收。
- 覆盖 reduced motion。
- 覆盖 light/dark/system。
- 覆盖多个同时可见标签页的主题 storage 同步。
- 覆盖 Modal portal、长内容 ScrollMask/ScrollShadow、关闭后的焦点回收。
- 覆盖 `IdRangeEditor` 从原生 `<dialog>` 迁移后的打开、提交、取消、Escape、焦点回收与滚动；业务源码不再调用 `showModal()`/`close()`。
- 覆盖 Select 分组、禁用项、description、长列表打开/键盘滚动和 string/number 值；按参考 patch 验证打开时不会强制滚动到已选项。
- 覆盖导出校验三条路径：无 issues 直接导出；有 issues 返回修改时不导出且 dirty 不变；明确确认“仍然导出”后成功下载并置 clean。
- 覆盖 4 个 Design Button 表面和 8 个含独立子操作的非 pressable Card，确认没有嵌套交互控件或伪造 `role=button`。
- 覆盖 persistent asset preview URL 的替换、删除、重复导入与 Provider 卸载；覆盖四个图片尺寸读取调用者的成功、加载失败和取消路径，瞬时 URL 每条路径均只 revoke 一次。
- 覆盖 375、768、1280、1920、2560 像素宽度。
- 不要求当前草案 UI 像素等价；按参考项目成熟布局、焦点、响应式和反馈模式验收。
- 扫描确认业务源码除 `beforeunload` 外没有直接 `alert`/`confirm`。
- 所有 agent 启动的 Playwright/浏览器会话、开发/静态服务器和后台进程在阶段结束时关闭。
- 可配置的浏览器、Playwright、npm/pnpm 等任务专用缓存指向 `.tmp/cache/<tool>/` 并随阶段清理；不得未经明确授权清理用户级或其他项目共享缓存。
- 临时截图、trace、脚本、日志和生成目录统一写入项目根 `.tmp/`；明确交付物写入文档指定路径。`.tmp/` 普通临时文件在阶段结束时保留并列入交接，不阻塞任务完成，用户后续另行要求时再清理。

## 6. 干净安装验证

- 以下步骤用于依赖和 patch 的可重现性验收；现有 `node_modules` 不能替代干净安装。
- [ ] 需要安装副本或临时移动现有 `node_modules` 时，只使用已解析并确认位于项目根 `.tmp/` 下的路径；
- [ ] 全新 pnpm 安装；
- [ ] `pnpm why @heroui/input @heroui/select`：两项都由目标源码直接调用；`client-only` 不作为目标直接依赖（D15）；
- [ ] `package.json` 登记的 Patch 与矩阵逐项一致，复制内容与固定参考提交一致；
- [ ] lockfile 记录对应 patch hash，安装没有 patch 应用失败；
- [ ] 记录实际版本、提供链和顶层解析；
- [ ] TypeScript、focused Prettier、diff check 和 build 全部在干净树运行；
- [ ] 运行时矩阵至少执行代表性全流程；
- [ ] 关闭安装/验证相关后台进程并清理本任务专用缓存；保留 `.tmp/` 中的安装/验证文件并列入交接，不阻塞任务完成，用户后续另行要求时再清理。

Windows 文件操作必须在 PowerShell 内使用已解析的绝对路径；用户后续要求清理时，递归移动/删除前先确认目标位于项目根 `.tmp/` 内，且只包含本次任务创建的内容。

## 7. Task 1 实际基线结果（2026-07-31）

- 输入、固定哈希、规范化状态、导出 JSON、entry manifest、LICENSE 和资产 SHA-256 位于 `fixtures/resource-pack/**`，使用说明与逐项结果见 `fixtures/resource-pack/README.md`。
- 目标仓库基线为 `33edb38d654650b73bfe076a2856bcdf18f5bc5f`，固定参考提交为 `d3b06126676430943856dfadc0bc8170920f9f72`；浏览器为 Windows Chrome `150.0.0.0`。
- 七个正常输入完成真实 UI 导入/导出；异常 `characters` 形状、dirty 下失败/成功覆盖、导出返回/确认、`beforeunload` 与 LICENSE 三态均已实测。
- persistent URL 连续导入/卸载累计 create/revoke 为 `13/13`；当前空 LICENSE 也被当作资产创建 URL。
- transient URL 实测确认成功与尺寸取消路径会撤销；损坏图片在 `PortraitUploader`、`SpriteSetEditor` 和 `SpriteGrid` 留下未撤销 URL。`SpriteUploader` 损坏图片会撤销尺寸检查 URL，但仍继续写入损坏资产。
- 既有 console 基线：HeroUI `useButton` 的 `onClick` deprecated warning、`/favicon.ico` 404、开发模式 React DevTools info。
- 项目没有 test script 或自动化测试套件；Task 1 不作此类通过声明。

## 8. 完成门

- [x] 根 `/` 仍重定向到 `/info`，12 个编辑 URL 不变；
- [ ] `out/` 在不运行 Next Node server 的条件下可由静态服务器完整访问，所有 route 直接刷新通过；
- [ ] 固定夹具导入/导出语义等价；
- [x] 归档 entry 分类符合 truth table：`ResourceEx.json`、`LICENSE.md`、`__MACOSX/**`、`assets/**`、非 `assets/` 文件和目录分别走唯一 owner；
- [x] `ResourceEx` 进入编辑器前已完成 wire 校验与归一化，领域校验不依赖 Web Crypto；
- [x] 持久与瞬时对象 URL 生命周期各有唯一 owner，业务/Design 调用者不直接创建或 revoke；
- [x] 无 `src/`、旧 design barrel、本地 `cn` 或父级相对 import；
- [x] route 只组合 Screen，实体 CRUD 和选择/预览状态归 feature；
- [x] 无分离的 `setData`/`setHasUnsavedChanges` 调用；
- [ ] 当前草案 UI 已按专题设计替换，键盘、焦点、滚动和响应式验收通过；
- [ ] 无参考项目业务或旧浏览器兼容层；
- [ ] 已选非业务能力可追溯到固定参考文件，全部实质偏差已登记；
- [ ] 无仅为参考目录对称引入的无调用者模块、全局声明或依赖；
- [ ] 直接依赖最小且干净安装可重现；
- [ ] 所有实际检查结果写入本文档；
- [ ] 所有未验证区域明确列出；
- [ ] 源码目录和系统临时目录中无 agent 输出；Playwright/浏览器会话、服务、后台进程和本任务专用缓存均已关闭或清理；`.tmp/` 保留内容已列入交接且不阻塞任务完成。

## 9. Task 2 实际结果（2026-07-31）

- 根 `app/` 已建立 layout、providers、客户端 `/` → `/info` 跳转与 12 个 route shell；迁移期 shell 通过 app→src 双路径 alias 组合现有业务 owner。
- 当前新增直接依赖为 `@heroui/input@2.4.10`、`@heroui/select@2.4.10`。初次安装的 `client-only@0.0.1` 已按用户要求通过普通 `pnpm remove client-only` 移除顶层声明。
- `patches/` 恰好包含矩阵指定的 8 份 patch，逐份与固定参考提交 Git blob 比较一致；`package.json` 和 lockfile 均登记 8 项，安装没有 patch 失败。
- `pnpm exec tsc --noEmit` 退出码 0；Task 2 focused Prettier、`git diff --check` 与 `git diff --cached --check` 通过。
- `pnpm build` 退出码 0，静态生成根页、404 和 12 个业务 route；`out/` 含 14 个要求的 HTML、RSC payload、155 个 `_next` 文件、116 个自托管字体文件与 public icon。
- 静态服务器直接请求根页、12 个业务 route、一个 `_next` chunk、一个字体、public icon 与不存在路径，结果分别为预期的 200/404；不需要 Next Node server。
- Windows Chrome 逐个直接打开根页和 12 个业务 route：全部主文档响应为 200，标题为 `MetaMystia ResourceEx Editor`，业务标题正确；根页最终路径为 `/info`，没有新增 console error 或失败请求。既有首次 `/favicon.ico` 404 仍按 Task 1 baseline 记录。
- 源码/config 扫描未发现 Server Action、请求态 cookies/headers、Route Handler、rewrites、redirects、proxy、standalone、assetPrefix 或 intercepting routes。
- 按用户直接要求保留迁移前 `SKIP_LINT` 环境变量分支并登记 D14；此前成功构建未设置该变量，未仅因还原动作重复构建。
- Task 2 结束时已关闭 Playwright/Chrome 会话与静态服务器；普通 `.tmp/task2-*` 脚本、日志和 Playwright 证据保留并列入交接，专用 cache 与本阶段生成的 `.next`/`out` 清理。

## 10. Task 4 实际结果（2026-07-31）

- 迁移前 RED：`app/design/preferences`、`app/design/theme`、目标 UI leaf、Textarea 和 evaluation owner 均不存在；旧 Design barrel/utils/hooks import 涉及 78 个文件；源码存在 12 个 `.surface-pressable` 与 `IdRangeEditor` 的 `showModal()`/原生 `<dialog>`。
- 迁移后结构扫描：旧 barrel/utils/hooks、本地 `cn`、`.surface-pressable`、原生 `<dialog>` 和 `showModal()` 均为 0；Task 6 已在全部 caller 迁移后删除 `src/design` 最后保留的 Select/Navbar owner。
- 从固定参考提交同步选中 Design/theme 闭包，并按 D02/D03/D08/D10/D11、evaluation 类型 owner 与静态资源根路径应用适配；未迁入 `accountSync` 和无调用者 wrapper。
- 4 个原生 pressable surface 已改为 Design Button；8 个含独立子动作的容器已改为非 pressable Card，选择动作继续由独立原生按钮承担。`IdRangeEditor` 已使用 Design Modal，layout 已提供 `#modal-portal-container`。
- 12 个业务 shell 已移入 `app/(pages)/<route>/page.tsx`；根 `app/page.tsx` 与根 layout 保持原 owner，不新增 group layout，route export 内容和 URL 不变。
- `pnpm exec tsc --noEmit`、focused Prettier、`git diff --check`、`git diff --cached --check` 均退出码 0。
- late fixes 后主代理重跑 production build：普通 sandbox 仍只因 `fonts.loli.net` DNS `ENOTFOUND` 失败，受控网络重试退出码 0；最终汇总为 16 个静态页面，包含全部 12 个业务 route、根与 404。
- Playwright 逐一直接访问 12 个业务 URL，主文档均为 200、标题一致且业务 heading 正确。唯一 console error 是既有 `/favicon.ico` 404 baseline。
- Playwright 验证 light/dark、system dark、系统媒体实时切回 light、对应 `colorScheme`/`theme-color`，以及两个同时打开页面的 dark/light storage 同步均正确。
- reduced-motion 独立 context 中媒体查询为 `true`：Button 的 computed `transition-property: none`（保留的 `0.25s` duration 不会驱动过渡）、Card/Modal/Modal close button 的 transition duration 为 `0s`，Button/Card/Modal 的 animation 均为 `none 0s`。源码同时确认 wrapper 以 `disableAnimation ?? isReducedMotion` 关闭 HeroUI 动画；Dropdown/Popover/Tooltip 即使传入自定义 motion props，也由同一 `disableAnimation` 契约关闭，因此没有 CSS duration 缺陷。
- IdRange Design Modal 实测位于 `#modal-portal-container`，375×420 视口下 scroller 为 `clientHeight=268`、`scrollHeight=282` 且可滚至 `scrollTop=14`；Escape 可关闭。首次实测发现焦点未回收，增加 trigger ref 后复测 `focusReturned=true`，active element 文本为“签名”。
- 以每个场景独立 page 绕开 dirty `beforeunload` 后，4/4 Design Button 均为原生 `button[type=button]`、`tabIndex=0` 且没有嵌套交互后代；8/8 Card 均为无 `role` 的 `DIV`、`tabIndex=-1`，选择动作是无嵌套交互后代的独立原生按钮。Food/Recipe 各含 3 个同级按钮，其余 Card 含 2 个，未产生嵌套按钮。
- Beverage、Clothes、Ingredient、Merchant 与对话 tree/flat 两种 Card 的删除按钮均由键盘 Tab 获得 `:focus-visible`，稳定后 `opacity=1`、`pointer-events=auto`；390×844、`hasTouch=true` 的窄屏 context 中 6/6 均为 `:focus-within`、中心 hit target 正确且 `tap({ trial: true })` 通过。
- Modal 调用者静态枚举为 Announcement、ExportValidation、FoodPreview、IdRange、AssetPicker 共 5 个。Announcement、IdRange、AssetPicker 的真实业务入口均验证 `role=dialog`、`aria-modal=true`、`#modal-portal-container`、Escape 关闭和焦点回收。对 ExportValidation 与 FoodPreview 的最终隔离验证不再经过业务触发或 local state：两个独立临时 route 首屏分别无条件渲染带固定 warning 的真实 ExportValidationDialog，以及 `foodId={0}`、`isOpen` 的真实 FoodPreviewDialog；两路均响应 200、`dialogCount=1`，具有 `role=dialog`、`aria-modal=true`、`data-open=true`、wrapper、backdrop 和 dialog 内初始焦点，内容分别包含固定 warning 与 `Food ID: 0`。console 只有开发模式 React DevTools info，`pageerror`、失败请求和 4xx/5xx response 均为空。probe 的 `onClose` 明确为 no-op，因此 Escape 后仍保持打开是受控 harness 的预期，不记作业务关闭通过；Modal wrapper 的 Escape 与焦点回收由其余三个真实入口覆盖。该结果把此前未观察到 dialog 的现象隔离在业务触发/自动化设置边界，而不是 caller render、Modal wrapper 或 portal。所有临时 route 均已删除。
- Playwright 会话与开发服务器已关闭，`.playwright-cli`、`.next` 和 `out` 已按阶段清理；`.tmp/task4-implementer-report.md` 为明确交付物，保留。

## 11. Task 5 实际结果（2026-08-01）

- 首轮独立审查未通过，明确发现五类缺口：实体叶子/嵌套 union 未完成 path-aware wire 校验；validation 与异步导出未绑定同一 revision；asset folder 与 Object URL 提交存在旧闭包/失败回滚风险；`CORE`/`DLC1` 保留 Label 缺少不可绕过的导出硬门；非 `assets/` 引用集不是从最终 trim 后的 JSON 视图生成。修复轮完成后才重新执行本节证据，当前等待主代理复核。
- `resource.d.ts` 已按 character、dialogue、event、items、merchant、mission 与顶层 ResourceEx 拆成无 barrel 的领域叶子；reader-only `IResourcePackWire`、空白创建、归一化、纯序列化、纯校验和资产引用收集均已进入 `app/domain/resourcePack/**`。旧 Context、类型、常量和 context utilities 在 caller 清零后删除。
- `.tmp/task5-domain-harness.mjs` 对七个固定正常输入和扩展 wire 路径执行确定性检查：规范化 JSON、序列化 JSON 字节、输入不变、ZIP entry 顺序/目录标志/字节、LICENSE manifest SHA-256、资产 SHA-256、十个集合、Branch `price` 既有裁剪、PackInfo/实体叶子/嵌套/discriminated union 的错误路径、合法 `null`/显式 `undefined`/未知扩展字段保留均通过。它还证明 trim 后的非 `assets/` 引用与导出 JSON 来自同一 export view，实际 ZIP 精确写入 `external/referenced.png` 且不写带空格 entry。该脚本是任务专用确定性 harness，不是仓库自动化测试套件。
- archive reader/writer 按 truth table 分类：JSON、LICENSE、macOS metadata、`assets/**`、非 assets 文件和目录各走唯一 owner；空 LICENSE 不生成输出 entry，非空 LICENSE 原字节写回。旧基线会把空 `LICENSE.md` 当资产创建第 7 个 persistent URL；新 reader 不把 LICENSE 放入文件快照，因此同一夹具改为 6 个 URL，这是 08 规定的 owner 修正，不是完全计数等价。
- Provider 只组合 domain、archive、asset 与 dirty；所有数据/LICENSE/文件/目录 mutation 同步增加 revision 并原子标脏，导入/创建在替换成功后增加 revision 并置 clean。Navbar 将 validation issues 与当时 revision 绑定；导出从 ref-backed 最新状态取得一致快照，写包/下载完成后仅在 revision 未变化时置 clean，保留 Label 无效/保留字的不可绕过硬门。页面不再接触 `setData` 或 `setHasUnsavedChanges`。
- `.tmp/task5-state-harness.mjs` 证明延迟 archive writer 只下载旧 revision 的一致快照且不会清除期间的新 dirty；过期 revision 在写包前被拒绝；`CORE`、`DLC1`、`Bad/Pack` 均在 writer 前被硬拒绝而 `Normal_Pack` 可导出；replace/update/remove/copy/move 的 URL 复用与撤销序列精确，Object URL 创建失败会回滚新 URL 且保留旧 maps，重复 revoke/dispose 不会重复调用底层 API。
- 真实静态页面验证：初始 clean；Info 字段编辑后 dirty；取消覆盖导入保持字段、dirty 与 URL `0/0`；接受非法 `characters` 输入得到带路径错误且字段/dirty/URL 不变；成功导入 missing-LICENSE 后名称替换、dirty=false、URL `6/0`；重复导入 empty-LICENSE 后累计 `12/6` 且首代全部撤销；创建空白包后恢复默认、dirty=false、累计 `12/12` 且全部撤销。
- 无 issues 导出真实下载 `NewPack-v1.0.0.zip`，导出前字段编辑为 dirty，下载后字段保留且 clean。SpriteUploader 集成覆盖有效图片确认上传、尺寸确认取消和损坏图片失败：累计 URL 分别为 `2/1`、`3/2`、`4/3`；每条 transient URL 均已撤销，唯一 persistent 预览 URL 保持 live。其余三个图片调用者通过静态 owner 扫描确认只调用同一 adapter；Task 3 已验证 adapter 自身成功、失败与 abort 清理。
- `URL.createObjectURL`/`URL.revokeObjectURL` 源码扫描仅剩 `useAssetStore.ts` 与 `readImageDimensions.ts`；domain 扫描没有 React、JSZip、FileSaver、Web Crypto、Blob URL、window 或浏览器事件依赖。
- 修复轮最新 `pnpm exec tsc --noEmit`、两个确定性 harness、focused Prettier、`git diff --check`、`git diff --cached --check` 与 focused browser probe 均退出码 0。普通 sandbox build 仍仅因 `fonts.loli.net` DNS `ENOTFOUND` 失败；受控网络 `pnpm build` 退出码 0，编译、类型检查、16/16 静态页与 export 均成功。
- 浏览器 console 唯一非预期业务外错误仍为既有 `/favicon.ico` 404；非法输入路径的 Provider `console.error` 与错误提示属于主动触发的预期失败。请求列表中的页面 RSC 预取均为 200。Python 临时服务器不实现 extensionless fallback，因此本阶段浏览器使用生成的 `/info.html`、`/ingredient.html`；完整 production-like 12 路由静态托管矩阵仍留在 Task 9。
- focused React/Playwright probe 证明 replace/import 后创建的 folder 不会被旧异步 uploader callback 回退，最终 snapshot 精确保留 `assets/`、`assets/imported/`、`assets/imported/new/`；替换与 StrictMode/unmount 路径创建的四个 persistent URL 均恰好 revoke 一次。未逐一在真实 UI 重跑四个图片 caller和资产复制/移动/文件删除；它们由统一 adapter owner、Task 3 图片 harness、SpriteUploader 集成和 state/assets transaction harness 覆盖，Task 9 仍须执行完整真实浏览器回归与干净安装/Patch 重现。
- Playwright 会话与临时开发/静态服务器已关闭，临时 probe route、`.playwright-cli` 以及最新 build 生成的 `.next`/`out` 均已删除。保留 `.tmp/task5-domain-harness.mjs`、`.tmp/task5-state-harness.mjs` 与 `.tmp/task5-implementer-report.md` 作为明确交接证据。

## 12. Task 7 实际结果（2026-08-01）

- `src/data/**` 的 10 个静态数据叶子已迁到 `app/domain/data/**`；9 个文件是字节级 rename，`ingredientPrefixes.ts` 只有 Prettier 换行，字面值与游戏术语未改。
- 12 个业务编辑器树已按规定切片顺序迁到 `app/features/resourceEditor/client/editors/**`，`dialog` 收口为 `dialogue`，`assetPaths.ts` 归入 `client/assets`。旧 `src/components/**`业务 owner、`src/data/**` 和 12 个 `src/app/<route>/page.tsx` 已在 caller 清零后删除。
- 12 个 `*EditorScreen.tsx` 组合布局、CRUD、索引选择、删除后调整、preview 和 collapse 状态；`app/(pages)/**/page.tsx` 只保留 Screen 组合，URL 不变。
- 真实浏览器逐路由覆盖 Info/Asset 和 ingredient/food/beverage/recipe/clothes/character/dialogue/merchant/mission/event 的新增、编辑、删除；对话额外验证角色引用与 `Branch price=2`，merchant/mission/event 额外验证角色、对话、奖励、post mission 和 post event。
- Event 的 HeroUI/Motion option 在 Playwright 鼠标 actionability 过程中曾因动画节点不稳定/脱离而超时；对应组件为原样 rename，键盘 focus + Enter 已成功写入 Dialog package 和 post-event。该项记为浏览器自动化指针操作限制，不声称鼠标自动化路径通过；Task 9 仍做真实交互总回归。
- 本阶段观察到的 `textValue` 透传 DOM warning、PortraitSelector 空 items 下 `selectedKeys="0"` warning 与 Event popover Motion `scale(NaN)` warning 均已定位到 Task 7 未改的 owner/既有路径；不作为 Task 7 新回归，Task 9 必须重新分类并记录最终 console 结果。
- `SpriteGrid.tsx` 与 `SpriteSet.tsx` 只调用 `readImageDimensions`；源码中 Blob URL API 仍只存在 persistent `useAssetStore.ts` 与 transient `readImageDimensions.ts` 两个 owner。
- 独立审查的唯一 Important 是 `DialogFormFields.tsx` import group 顺序，fix round 1 只重排 import 并由原审查者复核通过；最终无遗留 Critical/Important。
- `pnpm exec tsc --noEmit --incremental false`、focused Prettier、diff/owner/import 扫描通过；受控网络 `pnpm build` 成功生成 16/16 个静态页面。本项目没有自动化测试套件，本阶段不作套件通过声明。
- Playwright/服务器、`.playwright-cli`、`.next` 和 `out` 已清理。Task 8 仍保留 `src/app/{layout,providers,page}.tsx` 与 `src/app/globals.scss` 作为过渡 owner，将在根 App Router/样式/配置单路径收口后删除整棵 `src/`。

## 13. Task 8 实际结果（2026-08-01）

- `.bg-checkerboard` 完整规则以相同 SHA-256 从 `app/globals.scss` 下沉到 `app/features/resourceEditor/client/assets/styles.scss`，由唯一 `ResourceEditorProvider` 组合边界侧效加载；全部 8 个原 class caller 保留，定义和 import 各只一处。
- `app/globals.scss` 只增加选中参考的 `mobile-navbar-menu-scroll` 与 `--navbar-height: 4rem`；未迁入公告动画、driver、dotted underline、账户/站点偏移或兼容层。Navbar 实际 menu 接入该 utility，保留原 4rem 高度。
- `app/page.tsx` 保留客户端 `router.replace('/info')`，新增原生 `<noscript>` `/info` 链接；未使用 Next redirect/rewrites。根 layout/providers/Next config 无内容 diff，metadata、Noto `next/font/google` 变量、ThemeScript、App Shell、modal portal、static export、PEM 和 `SKIP_LINT` 分支保留。
- 删除 `src/app/{layout,providers,page}.tsx` 与 `src/app/globals.scss` 后 `src/` 已消失；`@/*` 仅指向 `./app/*`，Tailwind 只扫描 `app`。`tsconfig.include` 按用户决定保留五项宽范围原值和顺序，登记为 D17。
- 主代理最终 `pnpm exec tsc --noEmit --incremental false`、全部改动文件 focused Prettier、Task 5 domain/state 确定性 harness、两个 diff check、旧 owner/alias/barrel/parent import/domain/Object URL/server runtime 扫描通过。本项目没有自动化测试套件，未作套件通过声明。
- 普通 sandbox `pnpm build` 仍只因 `fonts.loli.net` DNS 失败；修复后受控网络构建退出码 0，编译、类型检查、16/16 静态页面与 export 成功。
- `out/` 由实现 `$uri` → `$uri.html` → `$uri/` 的纯静态 server 提供；12 个业务 URL 均直访 200 并渲染预期标题。JavaScript-enabled 根页落到 `/info`；JavaScript-disabled 根页停留 `/`、显示可用 fallback 链接，点击后 `/info` 200/H1。禁用 JS 时 chunk `FAILED csp` 是主动禁用脚本的预期结果。
- 390×844 移动菜单实测 `overscroll-behavior: contain`、`scrollbar-width: none`，打开后首项获焦，Escape 后回到 toggle。三个 Noto 变量为导出本地字体/fallback 值，modal portal 精确一个。
- 首轮独立审查发现 HeroUI height prop 写回同名 custom property 导致自引用循环的唯一 Important；fix round 1 删除该 prop，恢复 HeroUI `4rem` 默认。原审查者最终 Approved；修复后 root/Navbar/menu token 均为 `4rem`，Navbar 高 64px，menu `top=64px`、`height=780px`。
- JavaScript-enabled console 只有既有 `/favicon.ico` 404，0 warnings；当前 CSS/JS/字体/public asset/RSC 请求为 200，无 Next Node/API endpoint。所有 Task 8 与早期遗留 Playwright 会话已关闭，server、`.next`、`out`、`.playwright-cli` 和临时验证文件均已清理。
